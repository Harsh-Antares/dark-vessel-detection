# Data and heavy artifacts — never commit these
# (leading slash anchors to the repo root, so src/dark_vessel/data/ is NOT excluded)
/data/
outputs/tiles/
outputs/checkpoints/
outputs/predictions/
*.npz
*.pt
*.tif
*.tar.gz

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/

# OS / editor
.DS_Store
.idea/
.vscode/
*.tar

# Loose screenshots in repo root (move into assets/ with proper names)
/Screenshot*.png
