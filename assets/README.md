# Screenshots

Drop the two map screenshots here with these exact names so the main README
renders them:

- `map_tremiti.png`  — screenshot of `outputs/maps/map_tremiti.html`
  (zoom so the Isole Tremiti MPA boundary and the red dark-vessel dots inside
  it are clearly visible)
- `map_ravenna.png`  — screenshot of `outputs/maps/map_ravenna1.html`
  (the Po-delta reserves with dark detections)

On macOS: open the .html in a browser, press `cmd+shift+4`, drag a box over
the map area. The PNG lands on your Desktop — rename and move it here.

Optional: `pipeline.png` (a frame from your walkthrough video) and
`walkthrough.mp4` (the 2–3 minute screen recording).
