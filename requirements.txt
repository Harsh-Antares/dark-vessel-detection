# Core scientific stack
numpy>=1.24
pandas>=2.0
scipy>=1.10

# Deep learning (install the CUDA build of torch if you have an NVIDIA GPU;
# on Apple Silicon the default build supports the "mps" device)
torch>=2.1
torchvision>=0.16

# Geospatial raster + vector
rasterio>=1.3
pyproj>=3.5
geopandas>=0.14
shapely>=2.0

# Mapping / visualisation
matplotlib>=3.7
folium>=0.15

# Utilities
pyyaml>=6.0
tqdm>=4.65
scikit-learn>=1.3
